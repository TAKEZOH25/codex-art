document.addEventListener("DOMContentLoaded", () => {
    // Simple placeholder for MVP search logic
    console.log("Art Virtuoso Search loaded.");

    // Future implementation:
    // fetch('/search.json').then(r => r.json()).then(index => { ... })
});
